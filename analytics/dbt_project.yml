name: primeorder_analytics
version: 1.0.0
config-version: 2
profile: primeorder_analytics
model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
snapshot-paths: ["snapshots"]
clean-targets: ["target", "dbt_packages", "warehouse.duckdb"]

models:
  primeorder_analytics:
    staging:
      +materialized: view
      +schema: staging
    intermediate:
      +materialized: view
      +schema: intermediate
    marts:
      +materialized: table
      +schema: marts

seeds:
  primeorder_analytics:
    +schema: raw
    +quote_columns: false
